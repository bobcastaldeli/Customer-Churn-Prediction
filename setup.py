from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Custumer churn prediction with survival models',
    author='BobCasta',
    license='MIT',
)
